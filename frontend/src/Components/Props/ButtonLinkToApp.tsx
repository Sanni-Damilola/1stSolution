import React from "react";
import styled from "styled-components";

const ButtonLinkToApp = () => {
  return <Button>ButtonLinkToApp</Button>;
};

export default ButtonLinkToApp;

const Button = styled.div``;
